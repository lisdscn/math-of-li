---
theme: academic-paper
themeName: "学术论文"
title: "dg0401,02"
---

# 第四章 标架与曲面论基本定理

## 4.1 活动标架

令 $\mathbb{E}^3$ 的曲面 $S$ 的参数表示为 $\boldsymbol{r}=\boldsymbol{r}(u,v)$，参数曲面 $S$ 上的（光滑）向量场 $\chi(u,v)$ 是指：对于 $S$ 上任意一点 $\boldsymbol{r}(u,v)$，$\chi(u,v)$ 是从点 $\boldsymbol{r}(u,v)$ 出发的一个向量，且 $\chi(u,v)$ 光滑地依赖于参数 $(u,v)$。

当 $\chi(u,v)$ 为曲面 $S$ 在点 $\boldsymbol{r}(u,v)$ 的切向量时，$\chi(u,v)$ 为曲面 $S$ 的**切向量场**；
当 $\chi(u,v)$ 为曲面 $S$ 在点 $\boldsymbol{r}(u,v)$ 的法向量时，$\chi(u,v)$ 为曲面 $S$ 的**法向量场**。

曲面 $S$ 上的**活动标架**为以曲面上的点为原点的 $\mathbb{E}^3$ 的坐标系 $\{\boldsymbol{r}(u,v); \boldsymbol{x}_1, \boldsymbol{x}_2, \boldsymbol{x}_3\}$，其中 $(\boldsymbol{x}_1, \boldsymbol{x}_2, \boldsymbol{x}_3) \neq 0$；特别地，若 $\{\boldsymbol{x}_1, \boldsymbol{x}_2, \boldsymbol{x}_3\}$ 为单位正交标架时，则称 $\{\boldsymbol{r}(u,v); \boldsymbol{x}_1, \boldsymbol{x}_2, \boldsymbol{x}_3\}$ 为曲面 $S$ 的**正交活动标架**。

> **例**：设曲面 $S$ 的参数表示为 $\boldsymbol{r}=\boldsymbol{r}(u,v)$，对自然标架 $\{\boldsymbol{r}; \boldsymbol{r}_u, \boldsymbol{r}_v\}$ 施行 Schmidt 正交化，有：
>
> $$
> \boldsymbol{e}_1=\frac{\boldsymbol{r}_u}{\sqrt{\langle \boldsymbol{r}_u, \boldsymbol{r}_u \rangle}}=\frac{\boldsymbol{r}_u}{\sqrt{E}}\
> \boldsymbol{e}_2=\frac{\boldsymbol{r}_v-\langle \boldsymbol{r}_v, \boldsymbol{e}_1 \rangle \boldsymbol{e}_1}{|\boldsymbol{r}_v-\langle \boldsymbol{r}_v, \boldsymbol{e}_1 \rangle \boldsymbol{e}_1|}
> =\frac{E\boldsymbol{r}_v-F\boldsymbol{r}_u}{\sqrt{E(EG-F^2)}}
> $$
>
> 令
>
> $$
> \boldsymbol{e}_3=\boldsymbol{e}_1 \wedge \boldsymbol{e}_2
> =\frac{\boldsymbol{r}_u \wedge \boldsymbol{r}_v}{|\boldsymbol{r}_u \wedge \boldsymbol{r}_v|}
> =\boldsymbol{n}.
> $$
>
> 故 $\{\boldsymbol{r}; \boldsymbol{e}_1, \boldsymbol{e}_2, \boldsymbol{e}_3\}$ 为 $S$ 的一个（正定）正交标架（$(\boldsymbol{e}_1, \boldsymbol{e}_2, \boldsymbol{e}_3)>0$）。

**重要举例**：设 $\boldsymbol{r}(s)$ 是 $\mathbb{E}^3$ 的一条弧长参数曲线，$\{\boldsymbol{r}; \boldsymbol{e}_1(s), \boldsymbol{e}_2(s), \boldsymbol{e}_3(s)\}$ 为沿 $\boldsymbol{r}$ 正定的活动标架，令 $\{\boldsymbol{e}_1, \boldsymbol{e}_2, \boldsymbol{e}_3\}$ 为正交标架。

设曲线的单位切向量 $\frac{d\boldsymbol{r}}{ds}$ 在标架下分解为

$$
\frac{d\boldsymbol{r}}{ds}=a^1\boldsymbol{e}_1+a^2\boldsymbol{e}_2+a^3\boldsymbol{e}_3
$$

通过对标架 $\{\boldsymbol{e}_1, \boldsymbol{e}_2, \boldsymbol{e}_3\}$ 作适当正交变换，有 $a^1=1,\ a^2=a^3=0$。

令 $\boldsymbol{e}_1=\frac{d\boldsymbol{r}}{ds}$，并设

$$
\frac{d\boldsymbol{e}_i}{ds}=\sum_{j=1}^3 q_{ij}(s)\boldsymbol{e}_j(s),\quad i=1,2,3
$$

由对任意的 $i,j=1,2,3$，$\langle \boldsymbol{e}_i, \boldsymbol{e}_j \rangle=\delta_{ij}$，有 $q_{ij}+q_{ji}=0$。

沿 $\boldsymbol{r}$ 的任意一个新的正交标架 $\{\boldsymbol{r}; \widetilde{\boldsymbol{e}}_1, \widetilde{\boldsymbol{e}}_2, \widetilde{\boldsymbol{e}}_3\}$ 必然满足

$$
\widetilde{\boldsymbol{e}}_1=\boldsymbol{e}_1
$$

$$
\begin{bmatrix}
\widetilde{\boldsymbol{e}}_2\\
\widetilde{\boldsymbol{e}}_3
\end{bmatrix}
=
\begin{bmatrix}
\cos\theta & \sin\theta\\
-\sin\theta & \cos\theta
\end{bmatrix}
\begin{bmatrix}
\boldsymbol{e}_2\\
\boldsymbol{e}_3
\end{bmatrix},\quad \text{其中 } \theta \text{ 为 } s \text{ 的函数}.
$$

令

$$
\frac{d\boldsymbol{e}_1}{ds}=\widetilde{q}_{12}\widetilde{\boldsymbol{e}}_2+\widetilde{q}_{13}\widetilde{\boldsymbol{e}}_3
$$

发现

$$
\begin{bmatrix}
\widetilde{q}_{12}\\
\widetilde{q}_{13}
\end{bmatrix}
=
\begin{bmatrix}
\cos\theta & \sin\theta\\
-\sin\theta & \cos\theta
\end{bmatrix}
\begin{bmatrix}
q_{12}\\
q_{13}
\end{bmatrix},
$$

会有 $q_{12}{}^2+q_{13}{}^2=\widetilde{q}_{12}{}^2+\widetilde{q}_{13}{}^2,\ \kappa=\sqrt{q_{12}{}^2+q_{13}{}^2}$ 为曲线的曲率。

当 $\kappa>0$ 时，$q_{12},q_{13}$ 不同时为 0。$\exists \theta(s)$，使得

$$
\widetilde{q}_{13}=-\sin\theta\,q_{12}+\cos\theta\,q_{13}=0.
$$

而且，此时 $\widetilde{q}_{12}>0$。在这样的标架 $\{\widetilde{\boldsymbol{e}}_1,\widetilde{\boldsymbol{e}}_2,\widetilde{\boldsymbol{e}}_3\}$ 下最简单形式：

$$
\begin{cases}
\dfrac{d\widetilde{\boldsymbol{e}}_1}{ds} = \widetilde{q}_{12}\widetilde{\boldsymbol{e}}_2\\[6pt]
\dfrac{d\widetilde{\boldsymbol{e}}_2}{ds} = -\widetilde{q}_{12}\widetilde{\boldsymbol{e}}_1 + \widetilde{q}_{23}\widetilde{\boldsymbol{e}}_3\\[6pt]
\dfrac{d\widetilde{\boldsymbol{e}}_3}{ds} = -\widetilde{q}_{23}\widetilde{\boldsymbol{e}}_2
\end{cases}
$$

此时 $\{\widetilde{\boldsymbol{e}}_1,\widetilde{\boldsymbol{e}}_2,\widetilde{\boldsymbol{e}}_3\}$ 为 Frenet 标架。

---

## 4.2 自然标架的运动方程

首先引入如下记号：

令 $u^1=u,\ u^2=v$，即 $\boldsymbol{r}=\boldsymbol{r}(u^1,u^2)$

$$
\boldsymbol{r}_\alpha=\frac{\partial \boldsymbol{r}}{\partial u^\alpha},\ \alpha=1,2;\quad
\boldsymbol{r}_{\alpha\beta}=\frac{\partial^2 \boldsymbol{r}}{\partial u^\alpha \partial u^\beta},\ \alpha,\beta=1,2;
$$

$$
\boldsymbol{n}=\frac{\boldsymbol{r}_1 \wedge \boldsymbol{r}_2}{|\boldsymbol{r}_1 \wedge \boldsymbol{r}_2|};\quad
g_{\alpha\beta}=\langle \boldsymbol{r}_\alpha, \boldsymbol{r}_\beta \rangle,\ \alpha,\beta=1,2;\quad
b_{\alpha\beta}=\langle \boldsymbol{r}_{\alpha\beta}, \boldsymbol{n} \rangle=-\langle \boldsymbol{r}_\alpha, \boldsymbol{n}_\beta \rangle,\ \alpha,\beta=1,2;
$$

一般 $g_{11}=E,\ g_{12}=F,\ g_{22}=G,\ b_{11}=L,\ b_{12}=M,\ b_{22}=N$。

使用 **Einstein 求和约定**：若一个单项式中一个指标字母作为上标和下标各出现一次，则该式可视为对全体求和，省略求和号 $\sum$。

则会有

$$
d\boldsymbol{r}=\boldsymbol{r}_1 du^1+\boldsymbol{r}_2 du^2=\boldsymbol{r}_\alpha du^\alpha,
$$

$$
\mathrm{I}=g_{11}du^1du^1+2g_{12}du^1du^2+g_{22}du^2du^2 = g_{\alpha\beta}du^\alpha du^\beta
$$

$$
\mathrm{II}=b_{11}du^1du^1+2b_{12}du^1du^2+b_{22}du^2du^2 = b_{\alpha\beta}du^\alpha du^\beta
$$

令 $(g_{\alpha\beta})^{-1}=(g^{\alpha\beta}),\ g=\det(g_{\alpha\beta}),\ b=\det(b_{\alpha\beta})$。

将矩阵 $(g^{\alpha\beta})(g_{\alpha\beta})=(\delta^\alpha_\beta)$ 写成分量形式，$g^{\alpha\beta}g_{\beta\gamma}=\delta^\alpha_\gamma$。

那么令 $\{\boldsymbol{r}; \boldsymbol{r}_1, \boldsymbol{r}_2, \boldsymbol{n}\}$ 为自然标架，那么 $\boldsymbol{r}_{\alpha\beta}$ 与 $\boldsymbol{n}_\alpha$ 均可表示为 $\boldsymbol{r}_1, \boldsymbol{r}_2, \boldsymbol{n}$ 的线性组合。

设

$$
\boldsymbol{r}_{\alpha\beta}=\frac{\partial \boldsymbol{r}_\alpha}{\partial u^\beta}=\Gamma_{\alpha\beta}^\gamma \boldsymbol{r}_\gamma + C_{\alpha\beta}\boldsymbol{n},
$$

$$
\boldsymbol{n}_\alpha=\frac{\partial \boldsymbol{n}}{\partial u^\alpha}=D_\alpha^\beta \boldsymbol{r}_\beta + D_\alpha \boldsymbol{n},
$$

其中 $\Gamma_{\alpha\beta}^\gamma, C_{\alpha\beta}, D_\alpha^\beta$ 和 $D_\alpha$ 为待定系数。

会有：$C_{\alpha\beta}=\langle \boldsymbol{r}_{\alpha\beta}, \boldsymbol{n} \rangle = b_{\alpha\beta},\ D_\alpha=\langle \boldsymbol{n}_\alpha, \boldsymbol{n} \rangle=0$。

$D_\alpha^\beta g_{\beta\gamma}=\langle D_\alpha^\beta \boldsymbol{r}_\beta, \boldsymbol{r}_\gamma \rangle=\langle \boldsymbol{n}_\alpha, \boldsymbol{r}_\gamma \rangle=-b_{\alpha\gamma}$。

由 $D_\alpha^\beta g_{\beta\gamma}g^{\gamma\xi}=-b_{\alpha\gamma}g^{\gamma\xi},\ g_{\beta\gamma}g^{\gamma\xi}=\delta_\beta^\xi$，故有 $D_\alpha^\xi=-b_\alpha^\xi$（其中 $b_\alpha^\xi=b_{\alpha\gamma}g^{\gamma\xi}$）。

而 $\Gamma_{\alpha\beta}^\gamma g_{\gamma\xi}=\langle \boldsymbol{r}_{\alpha\beta}, \boldsymbol{r}_\xi \rangle$。

而

$$
\frac{\partial g_{\alpha\beta}}{\partial u^\gamma}=\langle \boldsymbol{r}_{\alpha\gamma}, \boldsymbol{r}_\beta \rangle+\langle \boldsymbol{r}_\alpha, \boldsymbol{r}_{\beta\gamma} \rangle
$$

那么

$$
\langle \boldsymbol{r}_{\alpha\beta}, \boldsymbol{r}_\gamma \rangle
=\frac12\left\{
\frac{\partial g_{\alpha\gamma}}{\partial u^\beta}
+\frac{\partial g_{\beta\gamma}}{\partial u^\alpha}
-\frac{\partial g_{\alpha\beta}}{\partial u^\gamma}
\right\}
$$

$$
\Gamma_{\alpha\beta}^\gamma g_{\gamma\xi}
=\frac12\left\{
\frac{\partial g_{\alpha\xi}}{\partial u^\beta}
+\frac{\partial g_{\beta\xi}}{\partial u^\alpha}
-\frac{\partial g_{\alpha\beta}}{\partial u^\xi}
\right\}
$$

即

$$
\Gamma_{\alpha\beta}^\gamma
=\frac12 g^{\gamma\xi}\left\{
\frac{\partial g_{\alpha\xi}}{\partial u^\beta}
+\frac{\partial g_{\beta\xi}}{\partial u^\alpha}
-\frac{\partial g_{\alpha\beta}}{\partial u^\xi}
\right\}
$$

与曲面第一基本形式的系数及其偏导数有关，故称 $\Gamma_{\alpha\beta}^\gamma$ 称为曲面 **Christoffel 符号**。

$$
\Gamma_{\xi\alpha\beta}=g_{\gamma\xi}\Gamma_{\alpha\beta}^\gamma
=\frac12\left\{
\frac{\partial g_{\alpha\xi}}{\partial u^\beta}
+\frac{\partial g_{\beta\xi}}{\partial u^\alpha}
-\frac{\partial g_{\alpha\beta}}{\partial u^\xi}
\right\}
$$

为曲面的**第二类 Christoffel 符号**。

那么曲面 $S$ 的自然标架 $\{\boldsymbol{r}; \boldsymbol{r}_1, \boldsymbol{r}_2, \boldsymbol{n}\}$ 的运动方程为

$$
\begin{cases}
\dfrac{\partial \boldsymbol{r}_\alpha}{\partial u^\beta}=\Gamma_{\alpha\beta}^\gamma \boldsymbol{r}_\gamma + b_{\alpha\beta}\boldsymbol{n},\quad \alpha,\beta=1,2; \quad (M_2)\\[6pt]
\dfrac{\partial \boldsymbol{n}}{\partial u^\alpha}=-b_\alpha^\beta \boldsymbol{r}_\beta,\quad \alpha=1,2; \quad (M_3)
\end{cases}
$$

由 $g_{11}=E,\ g_{12}=g_{21}=F,\ g_{22}=G$，得

$$
g^{11}=\frac{G}{EG-F^2},\quad g^{12}=g^{21}=\frac{-F}{EG-F^2},\quad g^{22}=\frac{E}{EG-F^2}
$$

用 $(u,v)$ 代替 $(u^1,u^2)$，则

$$
\Gamma_{11}^1=\frac{1}{EG-F^2}\left(\frac{G}{2}\frac{\partial E}{\partial u}+\frac{F}{2}\frac{\partial E}{\partial v}-F\frac{\partial F}{\partial u}\right),
$$

$$
\Gamma_{12}^1=\Gamma_{21}^1=\frac{1}{EG-F^2}\left(\frac{G}{2}\frac{\partial E}{\partial v}-\frac{F}{2}\frac{\partial G}{\partial u}\right),
$$

$$
\Gamma_{11}^2=\frac{1}{EG-F^2}\left(-\frac{F}{2}\frac{\partial E}{\partial u}-\frac{E}{2}\frac{\partial E}{\partial v}+E\frac{\partial F}{\partial u}\right),
$$

$$
\Gamma_{22}^1=\frac{1}{EG-F^2}\left(G\frac{\partial F}{\partial v}-\frac{G}{2}\frac{\partial G}{\partial u}-\frac{F}{2}\frac{\partial G}{\partial v}\right),
$$

$$
\Gamma_{12}^2=\Gamma_{21}^2=\frac{1}{EG-F^2}\left(-\frac{F}{2}\frac{\partial E}{\partial v}+\frac{E}{2}\frac{\partial G}{\partial u}\right),
$$

$$
\Gamma_{22}^2=\frac{1}{EG-F^2}\left(-F\frac{\partial F}{\partial v}+\frac{F}{2}\frac{\partial G}{\partial u}+\frac{E}{2}\frac{\partial G}{\partial v}\right).
$$
